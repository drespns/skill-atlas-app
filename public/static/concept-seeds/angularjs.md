<!-- skillatlas-tier: iniciacion -->
## MVC en el cliente

- Módulos `angular.module` y controladores `$scope`
- Plantillas HTML con directivas `ng-*`
- Enlace bidireccional `ng-model` y ciclo *digest*

## Directivas y filtros

- `ng-repeat`, `ng-if`, `ng-show` / `ng-hide`
- Filtros en plantillas (`date`, `currency`, personalizados)

<!-- skillatlas-tier: junior -->
## Servicios

- `factory`, `service`, `provider` y patrón singleton
- `$http` con promesas (`$q`) y interceptores

## Rutas legacy

- `ngRoute` o `ui-router` para SPA multipágina

<!-- skillatlas-tier: mid -->
## Buenas prácticas

- Evitar lógica pesada en controladores
- Componentización temprana hacia Angular moderno

## Migración

- Estrategias `ngUpgrade` y convivencia con Angular actual
- Cuándo reescribir frente a mantener legacy

<!-- skillatlas-tier: senior -->
## Mantenimiento

- Parches de seguridad en dependencias antiguas
- Rendimiento con listas grandes y *track by*
