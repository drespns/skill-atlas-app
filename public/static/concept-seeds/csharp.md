<!-- skillatlas-tier: iniciacion -->
## Sintaxis y tipos

- Tipos valor vs referencia, *boxing* mínimo
- `class`, `struct`, `record` y mutabilidad
- *Properties* con `get` / `init` / `set`

## Colecciones y LINQ

- `List<T>`, `Dictionary<K,V>`, `IEnumerable` perezoso
- Consultas LINQ: `Where`, `Select`, `GroupBy`, `Join`

<!-- skillatlas-tier: junior -->
## Asincronía

- `async` / `await`, `Task`, `ValueTask`
- `ConfigureAwait` en librerías reutilizables

## Patrones modernos

- Pattern matching con `switch` expresivo
- *Nullable reference types* y análisis estático

<!-- skillatlas-tier: mid -->
## Plataforma .NET

- `ASP.NET Core`: minimal APIs, middleware pipeline
- EF Core: `DbContext`, migraciones, consultas traducidas a SQL

## Memoria y rendimiento

- `Span<T>`, `Memory<T>` para buffers sin copias

<!-- skillatlas-tier: senior -->
## Arquitectura

- Inyección de dependencias integrada y *hosted services*
- Observabilidad con OpenTelemetry en servicios .NET
