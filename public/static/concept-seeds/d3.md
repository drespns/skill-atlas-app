<!-- skillatlas-tier: iniciacion -->
## Selección y datos

- `select`, `selectAll`, *data join* (*enter*, *update*, *exit*)
- Escalas (`scaleLinear`, `scaleTime`, `scaleOrdinal`)
- Ejes y generadores (`axisBottom`, etc.)

## SVG básico

- Formas `line`, `area`, `arc` y transiciones

<!-- skillatlas-tier: junior -->
## Layouts

- Jerarquías (`tree`, `treemap`, `pack`)
- Fuerzas (`forceSimulation`, enlaces y colisiones)

## Geo

- Proyecciones y `geoPath` para mapas

<!-- skillatlas-tier: mid -->
## Interacción

- `zoom`, `brush`, `drag` behaviors
- Transiciones coordinadas y *tweening*

## Rendimiento

- Canvas vs SVG para miles de nodos
- Virtualización y *quadtrees* en simulaciones

<!-- skillatlas-tier: senior -->
## Arquitectura

- Patrones modulares y reutilización en dashboards complejos
- Accesibilidad: texto alternativo y lectores de pantalla en visualizaciones
