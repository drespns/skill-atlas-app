<!-- skillatlas-tier: iniciacion -->
## Core e IoC

- Contenedor de inversión de control (`ApplicationContext`)
- Beans, alcances (`singleton`, `prototype`) e inyección por constructor
- Configuración Java, anotaciones y perfiles (`@Profile`)

## Spring Boot

- *Starters*, autoconfiguración y convención sobre configuración
- `application.yml` / propiedades por perfil (`dev`, `prod`)

<!-- skillatlas-tier: junior -->
## Web

- `@RestController`, mapeo HTTP y códigos de estado
- Validación con Bean Validation (`@Valid`)
- Spring Data JPA: repositorios, entidades y transacciones

## Seguridad

- Spring Security: autenticación, autorización y CSRF en APIs
- OAuth2 / JWT a alto nivel (resource server)

<!-- skillatlas-tier: mid -->
## Observabilidad

- Actuator: health, métricas y endpoints seguros
- Trazas distribuidas con Micrometer / OpenTelemetry

## Datos avanzados

- Consultas derivadas, `@Query` y proyecciones
- Migraciones con Flyway o Liquibase

<!-- skillatlas-tier: senior -->
## Cloud native

- Spring Cloud: config server, discovery, circuit breakers
- Despliegue en Kubernetes: sondas, recursos y *graceful shutdown*
