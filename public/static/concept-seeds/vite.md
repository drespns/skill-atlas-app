<!-- skillatlas-tier: iniciacion -->
## Dev server

- ESM nativo y HMR rápido en desarrollo
- `vite.config` con alias, plugins y `root` / `base`

## Build

- Rollup en producción, *tree-shaking* y code splitting
- Entrada múltiple y librerías empaquetadas (`lib mode`)

<!-- skillatlas-tier: junior -->
## Integración

- Plugins oficiales (React, Vue, legacy) y comunidad
- Variables `import.meta.env` y modos `development` / `production`

## Assets

- Importación de estáticos, URL resueltas y límites de tamaño

<!-- skillatlas-tier: mid -->
## Optimización

- *Dynamic import* y prefetch estratégico
- Análisis de bundle (`rollup-plugin-visualizer`)

## Monorepos

- Caché compartida y workspaces con PNPM / npm

<!-- skillatlas-tier: senior -->
## Producción

- Integración con SSR frameworks (Astro, Nuxt, etc.)
- Cabeceras de seguridad y *preload* de recursos críticos
